# Tupla são imutáveis.
lanche = ('hamburguer', 'suco', 'pizza', 'pudim')

for comida in lanche: # mostra cada elemento
    print(comida)

# for contador in range(0, len(lanche)):
#     print(lanche[contador])